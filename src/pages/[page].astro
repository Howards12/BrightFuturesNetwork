---
export async function getStaticPaths() {
  // List every route you want this file to generate.
  // Example: /about maps to params.page = "about"
  const pages = ["about", "contact", "privacy"];

  return pages.map((page) => ({
    params: { page },
  }));
}

const { page } = Astro.params;
---

<html lang="en">
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <title>{page} | Bright Futures Network</title>
  </head>
  <body>
    <main style="max-width: 720px; margin: 40px auto; font-family: system-ui;">
      <h1>{page}</h1>
      <p>
        This page is generated from <code>[page].astro</code>. Update the
        <code>pages</code> list in <code>getStaticPaths()</code> to control which routes are built.
      </p>
    </main>
  </body>
</html>